apiVersion: v1
kind: Service
metadata:
  name: {{ include "opensearch-governance-proxy.fullname" . }}
  labels:
    {{- include "opensearch-governance-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opensearch-governance-proxy.selectorLabels" . | nindent 4 }}
